# homebrew-aiuse

Homebrew tap for [`aiuse`](https://github.com/djbclark/aiuse).

```bash
brew tap djbclark/aiuse
brew install aiuse
```

Formula source of truth also lives in the main repo at
`packaging/homebrew/aiuse.rb` — keep this tap copy in sync on each release.
